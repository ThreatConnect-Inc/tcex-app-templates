# A short summary of the Template
summary: A basic template of a working Organization App.
# A full description of the Template and it's functionality
description: A basic template of a working Organization App.
# The provider of this template (name or email)
provider: ThreatConnect
contributor: ThreatConnect
# One or more templates to pre-download before loading current template.
# These templates will be pulled in the order that they are defined.
template_parents:
  - _app_common
# These are files that the App developer "should" not be
# editing and are typically safe to overwrite without prompt.
template_files:
  - job_app.py
  - run.py
# The current version number of this template
version: 1.0.0
